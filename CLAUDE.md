# CLAUDE.md — FG Event Platform 作業ルール

Claude Code がこのリポジトリで作業する際に必ず従うルール。
恒久的に有効な内容のみを置く。タスク履歴は `docs/TASK_HISTORY.md`、
現状調査は `docs/AUDIT_2026-07-29.md` を参照。

---

## 1. プロジェクト概要

FORMULA GYMKHANA 学生参加プラットフォーム。
事前/当日登録・QR名刺・スタンプラリー・景品交換・企業閲覧を提供する。

- **フロント**: `app/*.html` + `js/*.js`。**ビルド無しの素の HTML/JS**。GitHub Pages で配信。
  - `package.json` も `node_modules` も存在しない。外部依存は `js/vendor/` に手動ベンダリング済み。
- **バックエンド**: GAS Web App。**リポジトリ管理外**（ユーザーが GAS エディタで直接管理）。
- **データ**: Google スプレッドシート（MASTER + イベント別シート）。
  GAS のカラム定数が事実上のスキーマ定義。

### トークン設計

| トークン | 用途 | 保存先 |
|---|---|---|
| `cardToken` | 企業が学生情報を閲覧（card.html） | 学生マスター |
| `stampToken` | スタンプラリー専用 | `fg_stamp_token` cookie |
| `viewKey` | 企業の閲覧キー | `fg_company_view` cookie |
| `exchangeKey` | 景品交換スタッフキー | イベント別 CONFIG |

### 景品モデル

「N個集めるとM個選べる」（`prizeThreshold` / `prizeCount` / `exchangeKey`）。

### ローカルプレビューの起動方法

ビルド不要。静的サーバを立ててリポジトリルートを配信するだけ。
`.claude/launch.json` に `fg-static` として定義済み（`npx serve -p 8744`）。

Claude Code から起動する場合は `preview_start` に `{name: "fg-static"}` を渡す。
手動で起動する場合:

```bash
npx serve -p 8744 .
```

各ページは `http://localhost:8744/app/<name>.html` でアクセスする。
バックエンドは本番 GAS（`js/config.js` の `API_BASE_URL`）を直接叩くため、
**ローカルプレビューでも本番データに書き込みが発生する**。
フォーム送信を伴う確認は慎重に行うこと。

### コードの規模感

フロント全体は約 10,800 行（`app/*.html` + `js/*.js` + `style.css`）。
このうち **`app/admin.html`（1,295行）と `js/admin.js`（2,590行）で 3,885 行＝約 36%** を占める。

管理画面は単一ファイルに全セクション（ダッシュボード / 大学管理 / 企業管理 /
学生一覧 / フォーム管理 / エントリーリスト / 受付リスト / 設定）が同居しており、
**着手前に対象セクションを特定して範囲を絞ること**。
安易な全体読み込み・全体書き換えは影響範囲が読めなくなる。

### 最終的な運用形態

本プロジェクトは最終的に**開発者不在での運用**を目指している。
フロントは GitHub Pages、バックエンドは GAS のため特定の開発機には依存しないが、
以下は開発者がいないと実行できない。

- キャッシュバスター `?v=` の更新（§2）
- GAS の手動再デプロイ（§3）

したがって、**新機能は「GAS 再デプロイやコード変更なしに CONFIG シートから
調整できる」形を優先すること。**
ハードコードで済ませると、事務局が自力で運用できる範囲が狭まる。

既存の実装ではこの方針が以下の形で現れている。新機能でも踏襲すること。

- フォームの公開日時・締切（`schoolEntryFormOpenAt` / `schoolEntryDeadline` 等）
- メール件名・本文テンプレート（`schoolEntryMailSubject` / `schoolEntryMailBody` 等）
- 書類URL（`docUrlRulebook` / `docUrlPledge` / `schoolEntryApprovalUrl`）
- 景品条件（`prizeThreshold` / `prizeCount`）・スタンプ期間（`stampStartAt` / `stampEndAt`）
- 大学の並び順・女子ペアリング（`schoolRunningOrder` / `womenPairings`。JSON 保存）

いずれも管理画面から編集でき、GAS の `adminUpdateConfig` が任意キーに対応しているため、
**CONFIG で完結する設計にすれば新規 GAS エンドポイントすら不要**になる。

---

## 2. 絶対に守ること

- **JS / CSS を変更したら、それを参照する全 HTML のキャッシュバスター `?v=` を必ず更新する。**
  更新漏れは「直したはずが反映されない」の最頻出原因。
- **`docs/gas-patches/*.final.txt` を変更したら「GAS 再デプロイ必須」と明示的にユーザーへ伝える。**
  GAS はリポジトリ管理外のため、push しても自動反映されない。
  ユーザーが GAS エディタへ手動コピペ→再デプロイして初めて反映される。
- **`.claude/settings.local.json` は変更しない。**
- **稼働中イベントに影響する変更は、影響範囲を先に申告する。**
  イベント開催期間中の GAS 再デプロイは数秒の断を伴う。
- **push は実行前に必ずユーザーへ確認を取る。**
- **既存ファイルを修正する前に、変更対象の関数・変数・要素IDを呼び出し元/参照元まで
  検索し、影響範囲（他のシート・他のページ・他の関数）を洗い出してから編集に入る。**
  自動テストが無い本プロジェクトでは、これが唯一の安全網になる。
  リポジトリが大きくなるほど「一見無関係に見える箇所への波及」が起きやすく、
  実際に「存在しない要素IDを参照した判定が常に無効化されたまま気づかれない」
  「世代番号(loadGen_)の二重インクリメントで非同期処理の結果が握り潰される」
  といった、コードを読むだけでは見落としやすい不具合が発生している
  （2026-08-04、学生管理ページの食券集計未表示で発覚）。
  変更前に検索した内容（呼び出し元一覧・影響を受けるシート/要素等）は、
  実装に入る前にユーザーへ提示すること。
- **`docs/gas-patches/*.final.txt` を変更するPRは、常に1つずつ完結させる
  （マージ→即再デプロイ）。複数のGAS修正PRを未マージのまま並行して抱えない。**
  GASはリポジトリ管理外で「今どれが本番か」を機械的に確認する手段が無いため、
  並行して抱えると、片方だけ先に再デプロイした状態で `main` を後から
  まとめてコピペし直すと、未マージ側の修正が静かに巻き戻るリスクがある
  （2026-08-03、実際にこの状況が発生しかけて発覚）。

---

## 3. GAS ファイルの扱い

このリポジトリには GAS の `.gs` ファイルが**含まれていない**。

- リポジトリ内に `api.gs` / `admin.gs` が**存在する場合**: そのまま編集して差分を作る。
- **存在しない場合（現状）**: 修正後の完成版コードを `docs/gas-patches/` に出力する。
  - `api.gs.final.txt` … 本体（約 3000 行）
  - `admin.gs.final.txt` … 管理系
  - `testdata.gs.txt` … テストデータ生成

`.final.txt` が「GAS に貼り付けるべき最新版」であり、
GAS 側の実デプロイ内容と一致している保証はない。
乖離が疑わしいときはユーザーに確認する。

---

## 4. 設計上の確定事項（巻き戻し禁止）

過去に検討のうえ確定した判断。変更提案する場合は理由を明示すること。

- **GAS 通信のタイムアウトは `AbortController` ではなく `Promise.race` を使う（変更禁止）。**
  GAS のクロスオリジンリダイレクト後に abort signal が失われる iOS Safari の既知バグを
  回避するための実装。「モダンな書き方」への置き換えは **iPhone からのアクセス不能**を招く。
  対象は `js/api.js` の 2 関数:
  - `call_`（GET・タイムアウト 20 秒）
  - `postCall_`（POST・タイムアウト 120 秒。個人情報・ファイル添付用）

  どちらも `fetch(...).then(r => r.json())` と `setTimeout` で解決する Promise を
  `Promise.race` に掛ける。fetch 自体はキャンセルされない（await が解決されるだけ）が、
  これは意図的なトレードオフ。
  なお `postCall_` は `Content-Type` を付けない — CORS プリフライトを回避するため
  （GAS の `doPost` は生ボディを JSON として読む）。これも変更禁止。
- **当日受付コードは撤廃済み**（`register.html` は開放）。
- **イベント判定は日付駆動**。状態は `公開中 / 公開停止` の 2 値のみ
  （旧 `準備中`/`開催中`=公開中、`完了`=公開停止として互換扱い）。
  `getCurrentEvent_` は開催日内でも `公開停止`/`完了` を除外する＝早期停止スイッチ。
- **景品交換は「学生側確定方式」**。学生が `progress.html` で確定する。
  スタッフ側 `exchange.html` / `markPrizeExchanged` は補助・予備として残す（廃止しない）。
- **出場校エントリーは上書き型**。学校名で重複判定し、既存行を最新内容で置換。
  旧承諾書URLは `承諾書履歴` 列に蓄積。
- **書類URLは二段フォールバック**。config 設定値があれば JS で anchor href を上書き、
  未設定なら各 HTML ハードコードのデフォルトを使う。config が空でもリンク切れしない。
- **エントリー/受付リストは CSV 出力のみが正式**。テンプレ XLSX への貼り付け運用が前提。
  PDF/XLSX 直接出力は実装しない。
- **走行順は 男子A → 女子A → 男子B → 女子B → 男子C → 女子C**。
  男子は常に3ヒートまで。女子は大会ごとにCヒートの有無が変わる
  （2026-08-04にBのみ→Cありへ拡張）。女子ペアの`c`が1件も選ばれていなければ
  女子Cの計算は発火せず、既存の男子A/女子A/男子B/女子B/男子Cの式に一切影響しない。
  4ヒート以上は想定しない。
- **`activateStamp` に期間チェックは不要**（物理的に当日会場参加が前提）。
- **config キーの改名は禁止**。CONFIG シートに保存済みデータのマイグレーションが発生する
  （例: `schoolEntryApprovalUrl` は名前が不揃いだが維持する）。
- **大学統合機能は非アクティブ（`公開停止`/`完了`）なイベントを意図的に対象外とする。**
  `isEventInactive_` で判定し、そのイベントには一切書き込まない。接続できないだけの
  「要確認」（権限切れ・削除済みシート等）とは区別して大学統合履歴に記録する。
  過去の終了済みイベントまで遡って書き換える必要は無いという運用判断。

---

## 5. 実装状況と既知の課題

新機能の設計時・不具合調査時に前提として知っておくべき状態。
（詳細な調査経緯は `docs/AUDIT_2026-07-29.md`。ただし同ファイルもスナップショットであり、
実装が正であることは §8 の通り）

### 未実装のまま残っているもの

| 項目 | 現状 | 補足 |
|---|---|---|
| 書類URL設定の入力バリデーション | `app/admin.html:1004,1009,1014` の3入力とも `type="url"` のみ | Drive URL 形式の強制なし。誤 URL でも保存される。ただし §4 の二段フォールバックがあるため致命的ではない |
| GAS メール送信の共通ヘルパー化 | 3 関数が個別に config 読み込み・差し込み処理を持つ | `sendPreRegMail_` / `sendWalkInPassMail_` / `sendSchoolEntryConfirmMail_`（`api.gs.final.txt`）。メール文面の仕様変更時は 3 箇所すべての確認が必要 |
| 管理画面の権限ロール分離 | `ADMIN_KEY` 単一（`checkAdminKey_`） | 閲覧のみ / 編集可 の区別なし。キーを渡した相手は全操作が可能 |
| エントリー/受付リストの PDF・XLSX 出力 | CSV 出力のみ | §4 の通り**意図的に実装しない**。上記3項目とは性質が異なる |

### 技術的負債（自動検証が効かない領域）

- **テストコードが 0 件。CI も無い。** 回帰検知は手動確認のみ。
  （`docs/gas-patches/testdata.gs.txt` は GAS のテストデータ生成、
  `docs/screenshots/take-screenshots.js` はスクショ取得スクリプトであり、いずれもテストではない）
- **GAS 側とリポジトリの乖離を機械的に検出する手段がない。**
  `docs/gas-patches/*.final.txt` はあくまで「貼り付けるべき最新版」であり、
  実際に GAS にデプロイされている内容と一致している保証はない。
  §9 の CHANGELOG による再デプロイ記録が、事実上唯一の追跡手段。
- **キャッシュバスターの更新漏れを検出する仕組みがない。**
  JS を変更しても HTML の `?v=` を忘れると、ユーザー環境には旧版が配信され続ける。
  静かに壊れるため、§2 の手順を人間が守るしかない。

**結論として、このリポジトリでは変更の安全性を担保する自動的な仕組みが存在しない。**
変更は小さく、影響範囲を明示し、ユーザーが手で確認できる形で提示すること。

---

## 6. 命名規則

- **ページ**: `app/<name>.html` ↔ `js/<name>.js` の 1 対 1 対応。ケバブケース（`register-school`）。
- **GAS 内部関数**: アクションは `action<Name>_`、ヘルパーは末尾 `_`。
- **config キー**: キャメルケース（`schoolEntryDeadline`）。既存キーの改名は禁止（§4）。
- **キャッシュバスター**: `?v=YYYYMMDD<a-z>`（同日に複数回更新する場合は英字を進める）。

---

## 7. コミット規約

プレフィクス + 日本語本文。

| プレフィクス | 用途 |
|---|---|
| `feat:` | 機能追加 |
| `fix:` | バグ修正・文言修正 |
| `docs:` | ドキュメントのみ |
| `style:` | 見た目のみ（ロジック変更なし） |
| `chore:` | 雑務・整理 |
| `revert:` | 取り消し |

---

## 8. ドキュメントの読み方

**実装が正、ドキュメントは参考。** このプロジェクトは実装が先行しており、
ドキュメントは遅れて追従する。ドキュメントの記述を鵜呑みにしないこと。

信頼度の優先順位:

```
コード > docs/USER_MANUAL.txt > docs/HANDOFF_*.md > README.md > docs/NOTES.md / docs/CHANGELOG.md
```

| ファイル | 役割 |
|---|---|
| `README.md` | プロジェクト全体構成・ページ一覧 |
| `docs/USER_MANUAL.txt` | 事務局向け取扱説明書（配布物。実装と一致させる義務がある） |
| `docs/HANDOFF_<日付>.md` | セッション引き継ぎのスナップショット。追記せず、次回は新ファイル |
| `docs/AUDIT_<日付>.md` | 現状調査のスナップショット |
| `docs/CHANGELOG.md` | 変更記録（§9） |
| `docs/NOTES.md` | 設計メモ・論点。解決済み項目は削除せず「解決済み（日付）」を追記 |
| `docs/TASK_HISTORY.md` | 過去のタスク指示書・進捗履歴（アーカイブ。更新不要） |

> `handoff.md`（ルート直下）は存在しない。古い記述で参照されていたら
> `docs/HANDOFF_2026-07-24.md` に読み替えること。

---

## 9. 変更記録のルール

**`docs/CHANGELOG.md` は「GAS 再デプロイの要否と、現在デプロイ済みの GAS バージョン」の記録に絞る。**

- **コミット毎ではなく、機能単位で追記する。**細かい修正まで書かなくてよい
  （それは `git log` の役割）。
- **GAS 再デプロイを伴う変更は必ず記録する。**これが CHANGELOG の存在価値。
  再デプロイ要否が追えなくなると、GAS 側とリポジトリの乖離が検出不能になる。
- 新しいエントリは**先頭（上）**に追記する。

作業中に気づいた懸念・改善案・設計上の論点は `docs/NOTES.md` に追記する
（その場で直さなくてよい。記録して申し送る）。

---

## 10. 作業の進め方

1. 関連するコードを読んで現状を把握する（ドキュメントより先にコード）。
   **変更対象の関数・変数・要素IDが他のどこから参照・呼び出されているかを検索し、
   影響範囲を洗い出して提示してから編集に入る**（§2参照）。
2. GAS の変更が必要なら §3 の方針に従い `docs/gas-patches/` に出力する。
3. JS / CSS を変更したら §2 のキャッシュバスターを更新する。
4. 変更内容を要約し、コミットメッセージ案を提示する。
5. GAS 再デプロイが必要なら**明示的に伝える**。
6. 機能単位の変更なら `docs/CHANGELOG.md` に追記する（§9）。
7. **push は実行前にユーザーへ確認を取る。**

---

## 11. 作業上の基本姿勢

### 事実と推測を混ぜない

- ファイルの読み取り、コマンド実行、ツール呼び出しが**失敗した場合、推測で補完しない**。
  失敗した事実、対象のパス、エラーメッセージをそのまま報告し、そこで止まって指示を仰ぐ。
- 「おそらく〜だと思われる」「一般的には〜」で、読めなかった内容を埋めない。
  読めていないものは「読めていない」と書く。
- ファイル名・行番号・関数名・数値を出力するときは、**実際に確認したものだけ**を書く。
  確認していない参照を書かない。
- ドキュメントの記述を、コードを読んだ代わりにしない。
  コードを読んでいないなら「ドキュメントにはこう書かれている（未検証）」と明示する。

### 権限による拒否を迂回しない

- 権限設定やサンドボックスで拒否された操作を、別の手段で回避しようとしない
  （Read が拒否されたファイルを Bash の cat で読む、
  dangerouslyOverrideSandbox / dangerouslyDisableSandbox を使う等）。
- 拒否されたら、その事実を報告して指示を仰ぐ。
  設定の変更が必要と考える場合も、勝手に変更せず提案に留める。

### 不確かなときの扱い

- 判断に迷う箇所は勝手に決めず、質問としてまとめて提示する。
- 複数の解釈がありうる指示は、片方を選んで進める前に確認する。
- 確認済みの事実と、自分の推測・提案が混在する場合は、
  どちらがどちらか読み手に分かるように書き分ける。
